// introduction to javascript

// elaborate how console, document.querySelector, and document.getElementById work

// console.log() is a function that outputs a message to the console. It is commonly used for debugging purposes.

// document.querySelector() is a method that returns the first element within the document that matches a specified CSS selector, or group of selectors. It is commonly used for selecting elements on a web page.

// document.getElementById() is a method that returns the element that has the ID attribute with the specified value. It is commonly used for selecting elements on a web page.

// elaborate how variables work

// Variables are used to store data values in JavaScript. They are declared using the var, let, or const keywords, followed by a name and an optional value.

// tell about the comment in javascript

// Comments are used to add explanations or notes to the code. They are ignored by the JavaScript engine and do not affect the execution of the code. There are two types of comments in JavaScript: single-line comments (//) and multi-line comments (/* */).

// Node installation + npm installation + javascript code usage